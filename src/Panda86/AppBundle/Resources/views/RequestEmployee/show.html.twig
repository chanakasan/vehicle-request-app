{% extends '::base.html.twig' %}

{% block body -%}
    <h1>RequestEmployee</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Isowner</th>
                <td>{{ entity.isOwner }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('requestdata') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
