{% extends '::base.html.twig' %}

{% block body -%}
    <h1>RequestEmployee list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Isowner</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('requestdata_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.isOwner }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('requestdata_show', { 'id': entity.id }) }}">show</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% endblock %}
