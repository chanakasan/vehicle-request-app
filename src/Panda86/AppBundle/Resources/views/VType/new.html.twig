{% extends '::admin.html.twig' %}

{% block body -%}
    <h1>VType creation</h1>

    <form action="{{ path('vehicle-type_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('vehicle-type') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
