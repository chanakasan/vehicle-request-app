{% extends '::admin-ui.html.twig' %}

{% block body -%}
    <h3>Create a Vehicle Category</h3>

    <form action="{{ path('vtype_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('vtype') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
