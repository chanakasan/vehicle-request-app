{% extends '::admin.html.twig' %}

{% block body -%}
    <h1>Driver creation</h1>

    <form action="{{ path('driver_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('driver') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
